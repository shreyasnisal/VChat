class Group {
  String groupId, groupName, creationDate;
  int timestamp;
  List<dynamic> participants;

  Group(this.groupId, this.groupName, this.participants, this.creationDate, this.timestamp);
}
